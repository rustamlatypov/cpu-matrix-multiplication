0

Note that, in order to have some variable v defined for each thread, it is possible to:
- define a parallel region using #pragma omp parallel
- define the variable
- use #pragma omp for
In this way it would be possible to allocate the vector v, for each thread, just once.

--
Grading by:
dennis
